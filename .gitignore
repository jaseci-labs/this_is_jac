# Build output + deps
.jac/
node_modules/
__pycache__/

# Generated by capture.jac. benchmark.json + the shooter screenshots are kept
# as committed representative artifacts (re-run capture.jac to refresh them);
# the run manifest is machine-specific and not committed.
assets/captures/manifest.json
